# Analysis Tools

Generated from the FastMCP tool registry. Do not edit this file by hand; update
tool signatures/docstrings, then run `uv run python scripts/generate_tool_reference.py`.

## `find_dependencies`

Find transitive dependencies of a node. Returns nodes this depends on.

### Parameters

| Name | Required | Type | Default |
|---|---:|---|---|
| `name` | yes | `string` | `None` |
| `edge_kinds` | no | `array | null` | `None` |
| `max_depth` | no | `integer` | `5` |
| `response_shape` | no | `string` | `standard` |
| `token_budget` | no | `integer | null` | `None` |
| `cursor` | no | `string | null` | `None` |
| `limit` | no | `integer | null` | `None` |

### Input Schema

```json
{
  "properties": {
    "name": {
      "title": "Name",
      "type": "string"
    },
    "edge_kinds": {
      "anyOf": [
        {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Edge Kinds"
    },
    "max_depth": {
      "default": 5,
      "title": "Max Depth",
      "type": "integer"
    },
    "response_shape": {
      "default": "standard",
      "title": "Response Shape",
      "type": "string"
    },
    "token_budget": {
      "anyOf": [
        {
          "type": "integer"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Token Budget"
    },
    "cursor": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Cursor"
    },
    "limit": {
      "anyOf": [
        {
          "type": "integer"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Limit"
    }
  },
  "required": [
    "name"
  ],
  "title": "find_dependenciesArguments",
  "type": "object"
}
```

### Output Schema

```json
{
  "additionalProperties": true,
  "title": "find_dependenciesDictOutput",
  "type": "object"
}
```

## `find_dependents`

Find what depends on a node (impact analysis). Returns nodes that depend on this.

### Parameters

| Name | Required | Type | Default |
|---|---:|---|---|
| `name` | yes | `string` | `None` |
| `edge_kinds` | no | `array | null` | `None` |
| `max_depth` | no | `integer` | `5` |
| `response_shape` | no | `string` | `standard` |
| `token_budget` | no | `integer | null` | `None` |
| `cursor` | no | `string | null` | `None` |
| `limit` | no | `integer | null` | `None` |

### Input Schema

```json
{
  "properties": {
    "name": {
      "title": "Name",
      "type": "string"
    },
    "edge_kinds": {
      "anyOf": [
        {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Edge Kinds"
    },
    "max_depth": {
      "default": 5,
      "title": "Max Depth",
      "type": "integer"
    },
    "response_shape": {
      "default": "standard",
      "title": "Response Shape",
      "type": "string"
    },
    "token_budget": {
      "anyOf": [
        {
          "type": "integer"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Token Budget"
    },
    "cursor": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Cursor"
    },
    "limit": {
      "anyOf": [
        {
          "type": "integer"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Limit"
    }
  },
  "required": [
    "name"
  ],
  "title": "find_dependentsArguments",
  "type": "object"
}
```

### Output Schema

```json
{
  "additionalProperties": true,
  "title": "find_dependentsDictOutput",
  "type": "object"
}
```

## `rank_nodes`

Rank nodes by importance (in-degree or transitive dependents).

    Parameters
    ----------
    scope : list[str] | None
        Optional list of file paths or qualified names to restrict the ranking.
    kind : str | None
        Filter to a specific node kind (e.g. "class", "function").
    limit : int
        Maximum number of results to return.
    algorithm : str
        "in_degree" (default) for direct edge count, or "transitive" for
        recursive reverse-dependency count.

### Parameters

| Name | Required | Type | Default |
|---|---:|---|---|
| `scope` | no | `array | null` | `None` |
| `kind` | no | `string | null` | `None` |
| `limit` | no | `integer` | `20` |
| `algorithm` | no | `string` | `in_degree` |
| `response_shape` | no | `string` | `standard` |
| `token_budget` | no | `integer | null` | `None` |
| `cursor` | no | `string | null` | `None` |

### Input Schema

```json
{
  "properties": {
    "scope": {
      "anyOf": [
        {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Scope"
    },
    "kind": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Kind"
    },
    "limit": {
      "default": 20,
      "title": "Limit",
      "type": "integer"
    },
    "algorithm": {
      "default": "in_degree",
      "title": "Algorithm",
      "type": "string"
    },
    "response_shape": {
      "default": "standard",
      "title": "Response Shape",
      "type": "string"
    },
    "token_budget": {
      "anyOf": [
        {
          "type": "integer"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Token Budget"
    },
    "cursor": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Cursor"
    }
  },
  "title": "rank_nodesArguments",
  "type": "object"
}
```

### Output Schema

```json
{
  "additionalProperties": true,
  "title": "rank_nodesDictOutput",
  "type": "object"
}
```

