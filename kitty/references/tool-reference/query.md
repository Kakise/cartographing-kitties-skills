# Query Tools

Generated from the FastMCP tool registry. Do not edit this file by hand; update
tool signatures/docstrings, then run `uv run python scripts/generate_tool_reference.py`.

## `batch_query_nodes`

Query multiple nodes in one call. Returns found nodes with optional neighbors.

Each entry in ``nodes`` (and any nested ``neighbors[i].node``) carries a
``centrality`` field — a weighted-PageRank score in ``[0, 1]`` reflecting
structural importance. The cache is refreshed lazily on first read after
the graph changes and is stable within a graph version, so callers may use
it as a secondary sort key. See ``summarise_node`` in this module for the
canonical per-node schema.

### Parameters

| Name | Required | Type | Default |
|---|---:|---|---|
| `names` | yes | `array` | `None` |
| `include_neighbors` | no | `boolean` | `True` |
| `response_shape` | no | `string` | `standard` |
| `token_budget` | no | `integer | null` | `None` |
| `cursor` | no | `string | null` | `None` |
| `limit` | no | `integer | null` | `None` |

### Input Schema

```json
{
  "properties": {
    "names": {
      "items": {
        "type": "string"
      },
      "title": "Names",
      "type": "array"
    },
    "include_neighbors": {
      "default": true,
      "title": "Include Neighbors",
      "type": "boolean"
    },
    "response_shape": {
      "default": "standard",
      "title": "Response Shape",
      "type": "string"
    },
    "token_budget": {
      "anyOf": [
        {
          "type": "integer"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Token Budget"
    },
    "cursor": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Cursor"
    },
    "limit": {
      "anyOf": [
        {
          "type": "integer"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Limit"
    }
  },
  "required": [
    "names"
  ],
  "title": "batch_query_nodesArguments",
  "type": "object"
}
```

### Output Schema

```json
{
  "additionalProperties": true,
  "title": "batch_query_nodesDictOutput",
  "type": "object"
}
```

## `get_context_summary`

Get a compact grouped summary of nodes, optionally with edges between them.

Each entry under ``groups`` carries a ``centrality`` field — a
weighted-PageRank score in ``[0, 1]`` reflecting structural importance.
The cache is refreshed lazily on first read after the graph changes and is
stable within a graph version, so callers may use it as a secondary sort
key. See ``summarise_node`` in this module for the canonical per-node schema.

### Parameters

| Name | Required | Type | Default |
|---|---:|---|---|
| `file_paths` | no | `array | null` | `None` |
| `qualified_names` | no | `array | null` | `None` |
| `include_edges` | no | `boolean` | `False` |
| `max_nodes` | no | `integer` | `50` |
| `response_shape` | no | `string` | `standard` |
| `token_budget` | no | `integer | null` | `None` |
| `cursor` | no | `string | null` | `None` |

### Input Schema

```json
{
  "properties": {
    "file_paths": {
      "anyOf": [
        {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "File Paths"
    },
    "qualified_names": {
      "anyOf": [
        {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Qualified Names"
    },
    "include_edges": {
      "default": false,
      "title": "Include Edges",
      "type": "boolean"
    },
    "max_nodes": {
      "default": 50,
      "title": "Max Nodes",
      "type": "integer"
    },
    "response_shape": {
      "default": "standard",
      "title": "Response Shape",
      "type": "string"
    },
    "token_budget": {
      "anyOf": [
        {
          "type": "integer"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Token Budget"
    },
    "cursor": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Cursor"
    }
  },
  "title": "get_context_summaryArguments",
  "type": "object"
}
```

### Output Schema

```json
{
  "additionalProperties": true,
  "title": "get_context_summaryDictOutput",
  "type": "object"
}
```

## `get_file_structure`

Get all nodes in a given file with their relationships.

Each entry in ``nodes`` carries a ``centrality`` field — a weighted-PageRank
score in ``[0, 1]`` reflecting structural importance. The cache is
refreshed lazily on first read after the graph changes and is stable within
a graph version, so callers may use it as a secondary sort key. See
``summarise_node`` in this module for the canonical per-node schema.

### Parameters

| Name | Required | Type | Default |
|---|---:|---|---|
| `file_path` | yes | `string` | `None` |
| `response_shape` | no | `string` | `standard` |
| `token_budget` | no | `integer | null` | `None` |
| `cursor` | no | `string | null` | `None` |
| `limit` | no | `integer | null` | `None` |

### Input Schema

```json
{
  "properties": {
    "file_path": {
      "title": "File Path",
      "type": "string"
    },
    "response_shape": {
      "default": "standard",
      "title": "Response Shape",
      "type": "string"
    },
    "token_budget": {
      "anyOf": [
        {
          "type": "integer"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Token Budget"
    },
    "cursor": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Cursor"
    },
    "limit": {
      "anyOf": [
        {
          "type": "integer"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Limit"
    }
  },
  "required": [
    "file_path"
  ],
  "title": "get_file_structureArguments",
  "type": "object"
}
```

### Output Schema

```json
{
  "additionalProperties": true,
  "title": "get_file_structureDictOutput",
  "type": "object"
}
```

## `query_node`

Find a node by name or qualified name. Returns node details with immediate neighbors.

The returned ``node`` (and each ``neighbors[i].node``) carries a
``centrality`` field — a weighted-PageRank score in ``[0, 1]`` reflecting
structural importance. The cache is refreshed lazily on first read after
the graph changes and is stable within a graph version, so callers may use
it as a secondary sort key. See ``summarise_node`` in this module for the
canonical per-node schema.

### Parameters

| Name | Required | Type | Default |
|---|---:|---|---|
| `name` | yes | `string` | `None` |
| `response_shape` | no | `string` | `standard` |
| `token_budget` | no | `integer | null` | `None` |

### Input Schema

```json
{
  "properties": {
    "name": {
      "title": "Name",
      "type": "string"
    },
    "response_shape": {
      "default": "standard",
      "title": "Response Shape",
      "type": "string"
    },
    "token_budget": {
      "anyOf": [
        {
          "type": "integer"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Token Budget"
    }
  },
  "required": [
    "name"
  ],
  "title": "query_nodeArguments",
  "type": "object"
}
```

### Output Schema

```json
{
  "additionalProperties": true,
  "title": "query_nodeDictOutput",
  "type": "object"
}
```

## `search`

Full-text search across node names and summaries. Returns ranked results.

Each entry in ``results`` carries a ``centrality`` field — a
weighted-PageRank score in ``[0, 1]`` reflecting structural importance.
The cache is refreshed lazily on first read after the graph changes and is
stable within a graph version, so callers may use it as a secondary sort
key. See ``summarise_node`` in this module for the canonical per-node schema.

### Parameters

| Name | Required | Type | Default |
|---|---:|---|---|
| `query` | yes | `string` | `None` |
| `kind` | no | `string | null` | `None` |
| `limit` | no | `integer` | `20` |
| `response_shape` | no | `string` | `standard` |
| `token_budget` | no | `integer | null` | `None` |
| `cursor` | no | `string | null` | `None` |

### Input Schema

```json
{
  "properties": {
    "query": {
      "title": "Query",
      "type": "string"
    },
    "kind": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Kind"
    },
    "limit": {
      "default": 20,
      "title": "Limit",
      "type": "integer"
    },
    "response_shape": {
      "default": "standard",
      "title": "Response Shape",
      "type": "string"
    },
    "token_budget": {
      "anyOf": [
        {
          "type": "integer"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Token Budget"
    },
    "cursor": {
      "anyOf": [
        {
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Cursor"
    }
  },
  "required": [
    "query"
  ],
  "title": "searchArguments",
  "type": "object"
}
```

### Output Schema

```json
{
  "additionalProperties": true,
  "title": "searchDictOutput",
  "type": "object"
}
```

