# Reactive Tools

Generated from the FastMCP tool registry. Do not edit this file by hand; update
tool signatures/docstrings, then run `uv run python scripts/generate_tool_reference.py`.

## `graph_diff`

Return what structurally changed in the last indexing run.

    Args:
        file_paths: If provided, filter diff to only these files.
        include_edges: If True (default), include edge-level diff.

    Returns a dict with nodes_added, nodes_removed, nodes_modified,
    edges_added, edges_removed, and a summary.

### Parameters

| Name | Required | Type | Default |
|---|---:|---|---|
| `file_paths` | no | `array | null` | `None` |
| `include_edges` | no | `boolean` | `True` |

### Input Schema

```json
{
  "properties": {
    "file_paths": {
      "anyOf": [
        {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "File Paths"
    },
    "include_edges": {
      "default": true,
      "title": "Include Edges",
      "type": "boolean"
    }
  },
  "title": "graph_diffArguments",
  "type": "object"
}
```

### Output Schema

```json
{
  "additionalProperties": true,
  "title": "graph_diffDictOutput",
  "type": "object"
}
```

## `validate_graph`

Validate graph integrity and detect stale data.

    Args:
        scope: Qualified names or file paths to check. None = all
               (or last diff's changed nodes if available).
        checks: Subset of "dangling_edges", "orphan_nodes",
                "stale_annotations". None = all three.

    Returns a dict with ``passed``, ``issues``, and ``summary``.

### Parameters

| Name | Required | Type | Default |
|---|---:|---|---|
| `scope` | no | `array | null` | `None` |
| `checks` | no | `array | null` | `None` |

### Input Schema

```json
{
  "properties": {
    "scope": {
      "anyOf": [
        {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Scope"
    },
    "checks": {
      "anyOf": [
        {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "title": "Checks"
    }
  },
  "title": "validate_graphArguments",
  "type": "object"
}
```

### Output Schema

```json
{
  "additionalProperties": true,
  "title": "validate_graphDictOutput",
  "type": "object"
}
```

